name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - feature/**

jobs:
  validate:
    name: Test, Typecheck, Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.9.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 26.4.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Unit tests - messages
        run: pnpm exec vitest run src/shared/messages.test.ts --reporter=verbose

      - name: Unit tests - rules
        run: pnpm exec vitest run src/core/rules.test.ts --reporter=verbose

      - name: Unit tests - locale recommendation
        run: pnpm exec vitest run src/core/locale-recommendation.test.ts --reporter=verbose

      - name: Unit tests - settings editor
        run: pnpm exec vitest run src/options/settings-editor.test.ts --reporter=verbose

      - name: Unit tests - IP checker
        run: pnpm exec vitest run src/ip-check/checker.test.ts --reporter=verbose

      - name: Unit tests - IP providers
        run: pnpm exec vitest run src/ip-check/providers.test.ts --reporter=verbose

      - name: Unit tests - locale spoofing
        run: pnpm exec vitest run src/content/locale-spoofing.test.ts --reporter=verbose

      - name: Unit tests - storage
        run: pnpm exec vitest run src/storage/settings.test.ts --reporter=verbose

      - name: Typecheck
        run: pnpm typecheck

      - name: Build Chrome extension
        run: pnpm build:chrome

      - name: Build Firefox extension
        run: pnpm build:firefox

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Browser smoke test
        run: pnpm e2e:smoke
